<local:WszystkieViewBase x:Class="Firma.Views.WszyscyKontrahenciView"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            xmlns:restab="clr-namespace:Firma.ViewResources"
            xmlns:local="clr-namespace:Firma.Views"
            mc:Ignorable="d" 
            d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary Source="StyleZakladekResources.xaml"/>
    </UserControl.Resources>

    <Border Style="{StaticResource BorderStandardowy}">
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}" Style="{StaticResource TabelaStandard}" SelectedItem="{Binding Path=WybranyKontrahent, Mode=TwoWay}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Kod" CanUserSort="True" Width="100" Binding="{Binding Path=Kod}"/>
                <DataGridTextColumn Header="Nazwa" Width="200" Binding="{Binding Path=Nazwa}"/>
                <DataGridTextColumn Header="NIP/Pesel" Width="200" Binding="{Binding Path=NipLubPesel}"/>
                <DataGridTextColumn Header="Adres" Width="500" Binding="{Binding Path=KontrahentAdres}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Border>

</local:WszystkieViewBase>
